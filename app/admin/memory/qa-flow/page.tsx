import { resolvePandoraRuntimeSafetyConfig } from "@/lib/config/pandora-runtime-safety-config";
export default function OperatorMemoryQaFlowPage() { const runtime = resolvePandoraRuntimeSafetyConfig(); const steps = ["Review item loaded", "Decision verified", "Preview plan built", "Internal gate checked", "Persistence executed", "Memory item read back", "Source/patch/audit read back", "Browser view model verified"]; return <main><h1>Internal operator QA flow</h1><p><strong>Disabled by default</strong></p><p>No public persistence is enabled</p><p>No model calls, embeddings, or semantic retrieval are enabled</p><p>AU/story memory cannot become real-life evidence</p><section><h2>Runtime gate status</h2><ul><li>operator QA: {String(runtime.config.operatorQaFlowEnabled)}</li><li>approved review persistence: {String(runtime.config.approvedReviewPersistenceEnabled)}</li><li>public read: {String(runtime.config.publicMemoryReadEnabled)}</li><li>semantic retrieval: {String(runtime.config.semanticRetrievalEnabled)}</li></ul></section><section><h2>Authenticated/admin capability state</h2><p>Authenticated: false until server session resolves in deployment.</p><p>Internal operator: false by default.</p><p>Persistence operator: false by default.</p></section><section><h2>Namespace input/status</h2><input placeholder="namespace" disabled /></section><section><h2>QA inputs</h2><input placeholder="review item id" disabled /><input placeholder="decision id" disabled /><input placeholder="idempotency key" disabled /></section><section><h2>Step checklist panel</h2><ul>{steps.map((s) => <li key={s}>{s}: pending</li>)}</ul></section><button disabled aria-disabled="true">Run QA flow disabled</button></main>; }
