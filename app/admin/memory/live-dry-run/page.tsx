import { resolvePandoraRuntimeSafetyConfig } from "@/lib/config/pandora-runtime-safety-config";
export default function OperatorLiveDryRunPage() { const runtime = resolvePandoraRuntimeSafetyConfig(); const gates = runtime.config; return <main><h1>Operator live dry-run</h1><p><strong>Dry-run only</strong></p><p>No memory writes are performed</p><p>Public persistence remains disabled</p><p>Production ingest writes remain disabled</p><p>No model calls, embeddings, or semantic retrieval are enabled</p><p>Secrets are never displayed</p><p>Empty memory is acceptable for first setup if read APIs are healthy</p><section><h2>Namespace selector/input</h2><label>Namespace <input name="namespace" placeholder="real_life or au" /></label></section><section><h2>Session status</h2><p>Server session resolves through authenticated operator/admin context only.</p></section><section><h2>Runtime gate summary</h2><ul><li>persisted read: {String(gates.persistedMemoryReadEnabled)}</li><li>admin persistence: {String(gates.adminPersistenceConsoleEnabled)}</li><li>approved review persistence: {String(gates.approvedReviewPersistenceEnabled)}</li><li>operator QA: {String(gates.operatorQaFlowEnabled)}</li><li>public persistence: {String(gates.publicMemoryPersistenceEnabled)}</li><li>production ingest writes: {String(gates.ingestProductionWriteEnabled)}</li><li>public read: {String(gates.publicMemoryReadEnabled)}</li><li>model calls: {String(gates.modelCallsEnabled)}</li><li>embeddings: {String(gates.embeddingsEnabled)}</li><li>semantic retrieval: {String(gates.semanticRetrievalEnabled)}</li><li>GPT Actions: {String(gates.gptActionsEnabled)}</li><li>MCP: {String(gates.mcpEnabled)}</li></ul></section><section><h2>Environment safety summary</h2><p>Redacted status only; no secret values are shown.</p></section><section><h2>Read API probe summary</h2><p>Safe list/detail/source/patch/audit probes are read-only.</p></section><section><h2>Memory browser readiness summary</h2><p>Browser readiness uses read-only loading only.</p></section><section><h2>Admin persistence gate summary</h2><p>No button may execute persistence.</p></section><section><h2>Operator QA gate summary</h2><p>QA flow remains disabled unless internal/admin gates are present.</p></section><section><h2>Audit/idempotency checklist</h2><p>Audit required. Idempotency required. Append-only persistence remains required.</p></section><section><h2>Final dry-run status</h2><p>Pending server dry-run API execution by an authenticated operator.</p></section><section><h2>Blockers/warnings panel</h2><p>Blockers and warnings are returned by the GET-only dry-run API.</p></section></main>; }
