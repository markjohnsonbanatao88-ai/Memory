import type { PersistedMemoryBrowserSourceView } from "@/lib/api/persisted-memory-browser-dto";
export function MemorySourcePanel({ sources }: Readonly<{ sources: PersistedMemoryBrowserSourceView[] }>) { return <section className="section-card"><h2>Source/provenance panel</h2>{sources.length === 0 ? <p>No source records are available for the selected item.</p> : <ul>{sources.map((s) => <li key={s.id}><strong>{s.sourceType ?? "source"}: {s.sourceRef ?? s.id}</strong><p>{s.excerptPreview || "Sensitive evidence redacted by default."}</p><small>source id {s.id} · review {s.reviewItemId ?? "n/a"} · decision {s.decisionId ?? "n/a"}</small></li>)}</ul>}</section>; }
