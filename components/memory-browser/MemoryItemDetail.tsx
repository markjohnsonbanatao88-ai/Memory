import type { PersistedMemoryBrowserDetailView } from "@/lib/api/persisted-memory-browser-dto";
export function MemoryItemDetail({ detail }: Readonly<{ detail?: PersistedMemoryBrowserDetailView | null }>) { return <section className="section-card"><h2>Selected memory detail</h2>{detail ? <><h3>{detail.title ?? detail.id}</h3><p>{detail.textPreview}</p><p>Full memory text is redacted in this browser view by default.</p><dl><dt>Review item id</dt><dd>{detail.reviewItemId ?? "Not available"}</dd><dt>Decision id</dt><dd>{detail.decisionId ?? "Not available"}</dd><dt>Created</dt><dd>{detail.createdAt ?? "Not available"}</dd><dt>Updated</dt><dd>{detail.updatedAt ?? "Not available"}</dd></dl></> : <p>Select a memory item to inspect redacted detail.</p>}</section>; }
