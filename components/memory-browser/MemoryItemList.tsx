import type { PersistedMemoryBrowserItemView } from "@/lib/api/persisted-memory-browser-dto";
export function MemoryItemList({ items, selectedItemId }: Readonly<{ items: PersistedMemoryBrowserItemView[]; selectedItemId?: string }>) { return <section className="section-card"><h2>Memory items</h2>{items.length === 0 ? <p>No persisted memories found for this namespace and keyword-only filter.</p> : <ul>{items.map((item) => <li key={item.id} aria-current={item.id === selectedItemId ? "true" : undefined}><strong>{item.title ?? item.id}</strong><p>{item.textPreview}</p><small>{item.namespace} · {item.memoryKind ?? "uncategorized"} · patches {item.patchCount} · audit {item.auditCount}</small></li>)}</ul>}</section>; }
