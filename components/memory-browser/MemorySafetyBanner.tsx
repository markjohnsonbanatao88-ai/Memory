import type { PersistedMemoryBrowserSafetyView } from "@/lib/api/persisted-memory-browser-dto";
export function MemorySafetyBanner({ safety }: Readonly<{ safety: PersistedMemoryBrowserSafetyView }>) { return <section className="section-card" aria-label="Memory browser safety state"><h2>Read-only memory browser</h2><p>Authenticated and namespace-scoped</p><ul><li>No memory writes are available here</li><li>Semantic retrieval is not enabled</li><li>Model calls are not enabled</li><li>AU/story memory is not real-life evidence</li><li>Sensitive evidence is redacted by default</li></ul><p>Safety flags: readOnly={String(safety.readOnly)}, wouldWrite={String(safety.wouldWrite)}, wouldCallModel={String(safety.wouldCallModel)}, wouldEmbed={String(safety.wouldEmbed)}</p></section>; }
