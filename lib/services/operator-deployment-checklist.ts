import type { OperatorReadinessResult } from "@/lib/services/operator-readiness-contract";
export type OperatorDeploymentChecklistItem = { id: string; label: string; status: "pass" | "warning" | "blocked" | "pending"; blocker?: string; warning?: string; docsLink?: string; remediation: string };
export type OperatorDeploymentChecklistGroup = { id: string; label: string; items: OperatorDeploymentChecklistItem[] };
export function buildOperatorDeploymentChecklist(input: { readiness?: OperatorReadinessResult } = {}): { groups: OperatorDeploymentChecklistGroup[]; secretsIncluded: false } {
  const mk = (id: string, label: string, docsLink?: string): OperatorDeploymentChecklistGroup => ({ id, label, items: [{ id: `${id}_ready`, label: `${label} reviewed`, status: input.readiness?.ok === false ? "blocked" : "pending", blocker: input.readiness?.ok === false ? "readiness_blockers_present" : undefined, docsLink, remediation: "Use existing internal/admin gates only; do not paste secrets into tickets or logs." }] });
  return { secretsIncluded: false, groups: [mk("auth_session", "auth/session", "/docs/auth-session-and-runtime-gates"), mk("supabase_configuration", "Supabase configuration", "/docs/database-migrations"), mk("rls_rpc", "RLS and RPC migration readiness", "/docs/transactional-approved-review-memory-persistence-rpc"), mk("persisted_read_apis", "persisted read APIs", "/docs/authenticated-persisted-memory-read-api"), mk("admin_persistence_console", "admin persistence console", "/docs/private-admin-persistence-execution-console"), mk("operator_qa_flow", "operator QA flow", "/docs/operator-memory-qa-flow"), mk("memory_browser", "memory browser", "/docs/persisted-memory-browser-ui"), mk("audit_idempotency", "audit/idempotency", "/docs/transaction-idempotency"), mk("disabled_dangerous_features", "disabled dangerous features", "/docs/operator-live-readiness-and-gate-validation"), mk("future_semantic_retrieval", "future semantic retrieval phase", "/docs/retrieval-service")] };
}
